// this project is generated from @angular/cli and most of the time it usually follows CommonJS so the setup-jest will follow the CJS configuration 👇
// another clue is that there is no "type": "module" existed in the package.json file so it still default to CommonJS
import { setupZoneTestEnv } from 'jest-preset-angular/setup-env/zone';
setupZoneTestEnv();